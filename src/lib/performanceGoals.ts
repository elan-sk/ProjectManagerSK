// Metas globales de rendimiento individual (filosofía PSP): un único umbral
// para todo el equipo — no configurable por persona ni desde la UI todavía.
// Son valores de ejemplo: ajustalos acá si no reflejan lo que esperás.
export const PERFORMANCE_GOALS = {
  onTimeRate: 0.9,
  tasksCompleted: 10,
  firstTryApprovalRate: 0.8,
};
